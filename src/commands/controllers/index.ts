import { textController } from "./text";
import { commandsController } from "./cmd";
// import {} from './callback_query'

export const controllers = {
    text: textController,
    commands: commandsController 
}